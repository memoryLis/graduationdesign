server:
  port: 8080
hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall
    password: hmall123
    tokenTTL: 30m
  auth:
    excludePaths:
      - /search/list
      - /search/categories
      - /users/login
      - /users/register
      - /items/page
      - /hi